function love.conf(t)-- cant get it working not needed so fine if needed to change the window size is possible to do in main 
    t.window.width = 800                
    t.window.height = 600               
end
